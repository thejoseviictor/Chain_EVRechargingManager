version: "3.8"

services:
  vehicle:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: vehicle_client
    env_file:
      - .env
    network_mode: host
    restart: on-failure
